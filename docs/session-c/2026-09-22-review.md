# Session C continuation — 2026-09-22

Status: IN PROGRESS; not accepted for Session D or release.

## Baselines and evidence

- Current main read from origin: `5d126f0db2c061c10733ebd0cbb17be973c6e804`.
- Session B accepted baseline: PR #112, `d768fcd305eedaa7e9e0794865ddac582b0eb495`.
- Session C starting point: PR #113, `0541b45f25620754145a3d214d1f3e42187b2648`.
- Starting CI run 35298583286 failed branding, preventing SEO/lint/typecheck/unit/build from running. Database 35298583312 and Browser 35298583291 succeeded. CI used GitHub's PR merge ref, not a direct head checkout; do not mislabel the executed SHA.
- Prior PR #108/#110 are historical repair lines, not the current baseline. Face Liveness is excluded by the latest Session B handoff; selfie JPEG + CompareFaces remains the contract.

## Findings and repairs

1. Profile acceptance drift: before OPT-16, commit `40d00bd` passed `membershipBadgeSize="large"` from `chon-member-profile-screen.tsx` to `chon-member-photo.tsx`. `chon-membership-badge.tsx` mapped large to certificate and desktop geometry on both viewports: the 160x110 stage. Current C incorrectly used a 32px icon. Restore the certificate source and 110px height, retaining intrinsic aspect ratio rather than reintroducing the old wide wrapper. Keep top-left placement. Connect uses the requested 26px on both viewports; mini remains 12px.
2. Branding validator still asserted old 15/20px contexts. Its Connect guard accidentally matched the 15px photo-count assertion, so it did not actually check membership badge geometry. Guard the explicit mobile and desktop membership assertions separately from photo counts.
3. Admin Users omitted signup time in its list although the RPC already returns `signup_at` derived from Auth. Display this field with a machine-readable time element and Vietnam timezone; preserve backend ordering.
4. Production read-only RPC inspection on 2026-09-22 confirmed `order by u.created_at desc, p.id`. Its ledger contains `20260906163338_opt_ui_admin_signup_order`, but current B/C branches omitted the file. Restore the exact historical migration from `repair/opt-ui-repair-20260906`; no new schema contract and no production migration execution. Add a database regression for authoritative ordering and anonymous access denial.
5. Add warm-cache and cold-settings hero tests for 390/430/1280, exact responsive URL, decoded image, fallback/priority behavior and overflow.
6. Add Admin browser presentation tests at 390/430/1280 for list order preservation, signup time, detail media/selfie loading, share URL, overflow and ordinary-member route denial. These use explicitly synthetic API fixtures; they do not prove hosted Edge authorization or real signed URLs. Keep database and hosted verification evidence separate.

## Gates

Local source checks already run: branding, SEO/public-profile, OPT-15, Netlify Edge runtime PASS. Further checks and new-SHA CI are required before acceptance. No tests skipped/focused and no timeout inflation.

Remaining review: all required CI gates on the new candidate, screenshots including restored large badges and mobile Admin, real hosted media/Connect canaries for Session D, complete requirement matrix and release manifest in D/E. No Netlify deployment, main merge, production write, financial flag change or new Edge Function deployment in this continuation.
